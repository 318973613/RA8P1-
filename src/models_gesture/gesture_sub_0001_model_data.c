#include "../model_select.h"
/* MODEL_SELECT_GUARD */
#if 1
#include <stdint.h>
#include <stddef.h>

/* Gesture model is loaded from filesystem at runtime.
 * Keep only size symbol for compatibility. */
const size_t gesture_sub_0001_model_data_size = 436688;
const uint8_t gesture_sub_0001_model_data[16] = {0};
#endif /* 1 */
